import './Header.css';

const Header = () => {
    return (
        <header className="cabecalho">
            <h2 className="titulo-header">Previsão de falha sistema Pneumático</h2>
        </header>
    );
};

export default Header;
